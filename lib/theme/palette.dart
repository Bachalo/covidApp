//

import 'package:flutter/material.dart';

class Palette {
  static const Color primaryColor = Color(0xFF27074a);
  static const Color accentColor = Color(0xFF00abd1);
  static const Color accentColor2 = Color(0xFF160429);
  static const Color blueColor = Color(0xff23b6e6);
  static const Color greenierColor = Color(0xff02d39a);

  final kDefaultShadow = BoxShadow(
      offset: Offset(5, 5),
      blurRadius: 10,
      color: Colors.black.withOpacity(0.56));
}
